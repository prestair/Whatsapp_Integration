# Dependencies
node_modules/

# WhatsApp / Baileys auth & session data (SECRET — grants account access)
auth_info/
profiles/

# App config with credentials / deployment URLs
sheet_config.json

# Environment secrets (Supabase service-role key etc.) — never commit
.env
.env.*
!.env.example

# Cloudflare tunnel credentials
cloudflared-config.yml
*.cloudflared/

# Runtime data
message_history.json
uploads/

# Logs
*.log

# Build artifacts (generated by `npm run build:exe`)
build/
dist/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
